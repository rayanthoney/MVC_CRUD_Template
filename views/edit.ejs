<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="#">
    <title>100Devs MVC CRUD Template</title>
</head>
<body>
    <section>
        <h1>Welcome to your Edit Page!</h1>
        <section>
            <ul>
                <% itemList.forEach(item => { %>
                    <% if (clicked item == item in list) { %>
                     
                    <% } %>
                     
                 <li>
                    <form action="/edit/update/<%= item._id%>" method="POST">
                        <label for="textInput">Input Text</label>
                        <input type="text" name = "textInput" value="<%= item._textInput%>">
                        <br>
                        <label for="numInput" value="<%= item._numInput%>">Input Number</label>
                        <input type="number" name = "numInput" value = 0>
                        <br>
                        <button type="submit">Confirm</button>
                        <a href="/">Cancel</a>
                    </form>
                 </li>
                <% }) %>
            </ul>
        </section>

    </section>
    
</body>
</html>